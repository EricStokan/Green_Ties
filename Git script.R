
#You must firs install github
# This varies depending if you are on a Windows or a Mac

#Library for github
library(usethis)

#This email has to match
use_git_config(user.name = "EricStokan", user.email = "ericjstokan@gmail.com")

#Create a new repp and then click code
